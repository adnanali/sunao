<h1>Listing content_types</h1>

<table>
  <tr>
    <th>Type</th>
    <th>Data</th>
  </tr>

<% @content_types.each do |content_type| %>
  <tr>
    <td><%=h content_type.type %></td>
    <td><%=h content_type.data %></td>
    <td><%= link_to 'Show', admin_content_types_path(content_type.id) %></td>
    <td><%= link_to 'Edit', edit_admin_content_type_path(content_type.id) %></td>
    <td><%= link_to 'Destroy', admin_content_types_path(content_type.id), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New content_type', new_admin_content_type_path %>